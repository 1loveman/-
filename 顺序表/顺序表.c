#include"LNode.h"



int main() {
	//顺序表
	//线性结构采用顺序存储的方式存储的表，称为线性表
	//通过开辟一片连续数组存储空间存放同类型的有序数据


	//初始化顺序表

	//返回第i个元素

	//返回对应元素对应的下标

	//在第i个元素位置前插入一个新元素

	//删除对应位置的元素

	//返回顺序表的长度


	List PtrL = MakeEmpty();
	

	free(PtrL) ;
	return 0;
}